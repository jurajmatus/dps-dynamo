version: "2.0"
services: 
  key-value-store:
    build: .
    environment:
      NODE1_IP: "${NODE1_IP}"
      NODE2_IP: "${NODE2_IP}"
      RSYSLOG_IP: "${RSYSLOG_IP}"
      LOGGING_IP: "${LOGGING_IP}"
    ports:
      - "8080:8080"
    restart: always
    network_mode: "bridge"
