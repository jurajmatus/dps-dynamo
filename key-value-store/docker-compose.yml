key-value-store:
  build: .
  container_name: dynamo
  environment:
    GRAPHITE_HOST: 'localhost'
    COLLECTD_HOST: 'localhost'
    RSYSLOG_HOST: 'localhost'
  ports:
    - "8080:8080"
  restart: always
